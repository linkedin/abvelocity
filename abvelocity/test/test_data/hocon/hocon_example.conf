mapreduce=[
  {
    "override.mapreduce.map.memory.mb"=5120
    "override.mapreduce.reduce.memory.mb"=5120
  }
]
schedules=[
  {
    cluster=cat
    schedule="0 00 15 ? * *"
  }
]

# Some stuff
flowRandom=[
    {
        totalRuntimeSlaInMinutes=100,
        flowWatcherTags=['auto_generated', 'uxp', 'ump'],
        targetCluster="cat",
        totalRuntimeSlaWarningInMinutes=1000,
    }
]

dependencies=[
  {
    useSpark3=true
  }
]
